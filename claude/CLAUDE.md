# CLAUDE.md

## Tool Preferences

- Use `eza` instead of `ls` or `exa` for listing files and directories
- Use `eza --tree` for directory tree views
- Use `eza` with appropriate flags for file searching
